import { TradingViewWidgetConfig } from "./tradingview-types";

export const DEFAULT_WIDGET_CONFIG: TradingViewWidgetConfig = {
  symbol: "OANDA:XAUUSD",
  interval: "60",
  theme: "dark",
  autosize: true,
};